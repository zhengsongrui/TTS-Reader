# code_rules

## 开发规则

1. **代码精炼简练**：代码必须精简、清晰，避免冗余与过度设计。
2. **不添加未提及功能**：AI 不得加入用户未明确提及的功能。
3. **仅写最基本功能**：AI 只实现最基本、必要的功能，不自行扩展。
4. **使用 SCSS 写 CSS**：样式统一使用 SCSS 编写，不使用纯 CSS 或内联样式。
5. 不要管unpackage中的内容